// This files manages all the assets for us
// so i only have to import this file whenever i need an asset (like 'AssetsManager.Images')

import Images from './images';

const AssetsManager = {
    Images,
};

export default AssetsManager;